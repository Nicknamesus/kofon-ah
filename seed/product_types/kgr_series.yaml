# Promoted from 4 draft(s) under seed/drafts/.
# Merged LLM codes: kgr070, kgr_series
# Sources: 110.product_type, 129.product_type, 130.product_type, 131.product_type
code: kgr_series
name: Right-angle hypoid gearbox (KGR)
family: Spiral Bevel Gearbox
description: KGR series hypoid right-angle gearboxes from Kofon. Available in 1-stage
  and 2-stage configurations with ratios from 3:1 to 100:1. Features high precision
  (backlash ≤4 arcmin), high torque density, tapered roller bearings for high overturning
  moment capacity, and IP65 protection. Suitable for servo motor applications requiring
  compact right-angle power transmission.
product_page_url: https://www.kofon.com.cn/products_list/64.html
spec_schema:
  frame_size_mm:
    type: integer
    label: Frame size (mm)
  ratio:
    type: integer
    label: Ratio i
  nominal_torque_nm:
    type: number
    label: Rated output torque (Nm)
  peak_torque_nm:
    type: number
    label: Maximum acceleration torque (Nm)
  emergency_stop_torque_nm:
    type: number
    label: Emergency stop torque (Nm)
  rated_input_speed_rpm:
    type: integer
    label: Rated input speed (rpm)
  max_input_speed_rpm:
    type: integer
    label: Max input speed (rpm)
  no_load_torque_nm:
    type: number
    label: Mean no load running torque (Nm)
  backlash_arcmin:
    type: number
    label: Backlash (arcmin)
  torsional_rigidity_nm_per_arcmin:
    type: number
    label: Torsional rigidity (Nm/arcmin)
  max_radial_force_n:
    type: integer
    label: Max radial force (N)
  max_tilting_moment_nm:
    type: number
    label: Max tilting moment (Nm)
  rotational_inertia_kgcm2:
    type: number
    label: Rotational inertia (kg·cm²)
  efficiency_pct:
    type: integer
    label: Full loading efficiency (%)
  operating_temp_min_c:
    type: integer
    label: Operating temperature min (°C)
  operating_temp_max_c:
    type: integer
    label: Operating temperature max (°C)
  lubrication:
    type: string
    label: Lubrication
  mounting_position:
    type: string
    label: Mounting position
  protection_class:
    type: string
    label: Protection class
  service_lifetime_h:
    type: integer
    label: Service lifetime (h)
  weight_kg:
    type: number
    label: Weight (kg)
  stages:
    type: integer
    label: Number of stages
  noise_db:
    type: integer
    label: Noise (dB(A))
  frame_size:
    type: string
    label: Frame size
