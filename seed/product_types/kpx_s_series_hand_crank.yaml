# Promoted from 4 draft(s) under seed/drafts/.
# Sources: 113.product_type, 115.product_type, 123.product_type, 124.product_type
code: kpx_s_series_hand_crank
name: KPX-S Series Hand-Crank Input Reducer
family: Planetary gearbox
description: The KPX-S series is a hand-crank input planetary gearbox designed for
  manual backup operation when electrical power is unavailable. It ensures equipment
  can continue running in remote areas or emergency situations. Available in 1-stage
  and 2-stage configurations with a wide range of ratios, offering high reliability,
  simple operation, and standard backlash options.
spec_schema:
  ratio:
    type: integer
    label: Ratio
  stages:
    type: integer
    label: Number of stages
  nominal_torque_nm:
    type: number
    label: Rated output torque (Nm)
  peak_torque_nm:
    type: number
    label: Maximum acceleration torque (Nm)
  emergency_stop_torque_nm:
    type: number
    label: Emergency stop torque (Nm)
  rated_input_speed_rpm:
    type: integer
    label: Rated input speed (rpm)
  max_input_speed_rpm:
    type: integer
    label: Max input speed (rpm)
  backlash_arcmin:
    type: string
    label: Backlash (arcmin)
  max_radial_force_n:
    type: integer
    label: Max radial force (N)
  max_axial_force_n:
    type: integer
    label: Max axial force (N)
  rotational_inertia_kgcm2:
    type: number
    label: Rotational inertia (kg·cm²)
  noise_db:
    type: number
    label: Noise (dB(A))
  efficiency_pct:
    type: number
    label: Full loading efficiency (%)
  operating_temp_range_c:
    type: string
    label: Operating temperature range (°C)
  lubrication:
    type: string
    label: Lubrication
  mounting_position:
    type: string
    label: Mounting position
  service_lifetime_h:
    type: integer
    label: Service lifetime (h)
  weight_kg:
    type: number
    label: Weight (kg)
  noise_db_a:
    type: integer
    label: Noise (dB(A))
  operating_temp_min_c:
    type: integer
    label: Operating temperature min (°C)
  operating_temp_max_c:
    type: integer
    label: Operating temperature max (°C)
  frame_size_mm:
    type: string
    label: Frame size
  stage:
    type: integer
    label: Number of stages
  variant:
    type: string
    label: Variant (standard/precision)
