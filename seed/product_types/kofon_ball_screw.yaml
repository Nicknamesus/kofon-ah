# Promoted from 1 draft(s) under seed/drafts/.
# Sources: 46.product_type
code: kofon_ball_screw
name: Precision ball screw
family: Planetary Roller Screw
description: Kofon precision ball screw series. High-efficiency (≥90%) rolling-element
  linear actuator converting rotary to linear motion (or vice versa) with low friction,
  high positioning accuracy, high rigidity (via preload), high speed capability, and
  long service life. Available in multiple screw diameters (16–63 mm) and leads (4–10
  mm) with various ball diameters and circuit configurations.
product_page_url: https://www.kofon.com.cn/products_list/25.html
spec_schema:
  screw_diameter_mm:
    type: integer
    label: Screw outer diameter (mm)
  lead_mm:
    type: integer
    label: Lead (mm)
  ball_diameter_mm:
    type: number
    label: Ball diameter (mm)
  circuit_count:
    type: integer
    label: Number of circuit turns
  dynamic_load_capacity_kgf:
    type: integer
    label: Dynamic load capacity Ca (kgf)
  static_load_capacity_kgf:
    type: integer
    label: Static load capacity Coa (kgf)
  nut_outer_diameter_mm:
    type: number
    label: Nut outer diameter Dg6 (mm)
  nut_length_mm:
    type: integer
    label: Nut length L (mm)
  flange_diameter_mm:
    type: integer
    label: Flange diameter A (mm)
  flange_thickness_mm:
    type: integer
    label: Flange thickness T (mm)
  flange_width_mm:
    type: integer
    label: Flange width W (mm)
  flange_center_hole_mm:
    type: integer
    label: Flange center hole G (mm)
  flange_bolt_circle_mm:
    type: integer
    label: Flange bolt circle H (mm)
  mounting_hole_diameter_mm:
    type: number
    label: Mounting hole diameter S (mm)
  mounting_hole_counterbore_mm:
    type: number
    label: Mounting hole counterbore X (mm)
  mounting_hole_depth_mm:
    type: number
    label: Mounting hole depth Y (mm)
  oil_hole:
    type: string
    label: Oil hole thread
  rigidity_kgf_per_um:
    type: integer
    label: Rigidity (kgf/µm)
